from typing import Dict, Tuple
import numpy as np

node_coord_dict = Dict[str, Tuple[int, int]]
binary_image = np.array  # boolean array of dimension 2
coord = Tuple[float, float]
coord_int = Tuple[int, int]
